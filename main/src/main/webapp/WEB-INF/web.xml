<!DOCTYPE web-app PUBLIC
 "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
 "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://java.sun.com/xml/ns/javaee" xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd" id="WebApp_ID" version="2.5">
  <display-name>crowdfunding</display-name>
  <context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>classpath*:spring/spring-*.xml</param-value>
  </context-param>

  <!-- 监听器 -->
  <!--监听器:
        创建Spring框架的IOC容器,
        将IOC容器对象放在application域中
        ApplicationContext ioc = WebApplicationContextUtils.getWebApplicationContext(application)
        ServletContextListener监听器接口:
          监听ServletContext创建和销毁:
            如果监听到 ServletContextListener创建,就会创建IOC容器
            如果监听到 ServletContextListener销毁,就会销毁IOC容器-->
  <listener>
    <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
  </listener>
  <!--配置自己写的监听器类:StarSystemListener-->
  <listener>
    <listener-class>com.crowd.funding.listener.StarSystemListener</listener-class>
  </listener>
  
  <!-- 字符编码过滤器:
        只负责解决post的编码问题-->
  <filter>
    <filter-name>encoding</filter-name>
    <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
    <init-param>
      <param-name>forceEncoding</param-name>
      <param-value>true</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>encoding</filter-name>
    <url-pattern>/*</url-pattern>
    <!--过滤的规则-->
    <!--<dispatcher>FORWARD</dispatcher>-->
    <!--<dispatcher>REQUEST</dispatcher>-->
  </filter-mapping>

  <!--支持Restful风格,
        将Post请求转换为PUT请求或者DELETE请求,会读取隐含参数:"_method"-->
  <filter>
    <filter-name>HiddenHttpMethodFilter</filter-name>
    <filter-class>org.springframework.web.filter.HiddenHttpMethodFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>HiddenHttpMethodFilter</filter-name>
    <servlet-name>springmvc</servlet-name>
  </filter-mapping>


  <!--核心配置器-->
  <servlet>
    <servlet-name>springmvc</servlet-name>
    <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    <init-param>
      <param-name>contextConfigLocation</param-name>
      <param-value>classpath:spring/springmvc-context.xml</param-value>
    </init-param>
    <load-on-startup>1</load-on-startup>
  </servlet>
  <servlet-mapping>
    <servlet-name>springmvc</servlet-name>
    <!--拓展匹配-->
    <url-pattern>*.htm</url-pattern><!-- 主要用于页面跳转 -->
    <url-pattern>*.do</url-pattern><!-- 主要用于业务逻辑处理 -->
  <!--  精确匹配
  <url-pattern>/user/save</url-pattern>
    -->
  <!--  路径匹配
    <url-pattern>/user/*</url-pattern>
    <url-pattern>/*</url-pattern>-->
  <!--  默认匹配-->
  <!--  <url-pattern>/</url-pattern>-->

  </servlet-mapping>

  <!-- 会话超时时间 -->
  <session-config>
    <session-timeout>60</session-timeout>
  </session-config>

  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>
</web-app>
